export interface Budget {
    id: number;               // The unique identifier for the budget
    totalAmount: number;     // The total budget amount
    eventId: number;         // The ID of the associated event
  }
  